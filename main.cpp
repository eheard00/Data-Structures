/*******************************************************************************    PRG Libraries*******************************************************************************/#include <iostream>#include <fstream>#include "functions.h"using namespace std;int main(){/*******************************************************************************    Variables*******************************************************************************/    int recsize; //Declare var for size of array    int searchvalue;    int modifyvalue;    int addvalue;    int newvalue;    int oldvalue;    int index;    string function;/*******************************************************************************    File ingestion*******************************************************************************/    int rec[105];   //Declares record        ifstream input;    input.open("A1input.txt");        for(int i = 0; i < 105; i++) {        input >> rec[i];    }        input.close();/*******************************************************************************    Main Program*******************************************************************************/    cout << endl << "What would you like to do with the array? Type out your answer below."         << endl << "Output, Search, Modify, Add, Remove, or Exit?" << endl;    while(true) {        cin >> function;        cout << endl;                if(function == "Search" || function == "search") {            cout << "Enter the value you wish to search for: ";            cin >> searchvalue;            index = search(rec, 105, searchvalue);            if(index == -2) {                cout << "Search value is not in the array" << endl;            }            else {                cout << "Search value found at index: " << index << endl;            }        }         else if(function == "Modify" || function == "modify") {            cout << "Enter the index you'd like to modify and the new value: " << endl;            cout << "Index: "; cin >> index;            cout << "New Value: "; cin >> modifyvalue;            oldvalue = modify(rec, index, modifyvalue, 105);            cout << "Index " << index << " replaced with " << modifyvalue                << endl << "The value was " << oldvalue << "." << endl;        }        else if(function == "Add" || function == "add") {            cout << "Enter the value you'd like to add to the end of the array." << endl;            cout << "New Value: "; cin >> addvalue;            newvalue = add(rec, 105, addvalue);            cout << endl << "The value " << addvalue << " was added to the end of the array." << endl;        }        else if(function == "Output" || function == "output"){                for(int j = 0; j < 105; j++){   // Loops through record to output values.                    cout << rec[j] << endl;            }        }        else if(function == "Remove" || function == "remove") {            cout << "Enter the index ";            cin >> index;            remove(rec, index, 105);        }        else if(function == "Exit" || function == "exit") {            exit(0);        }        else {            cout << "Please select from the given values" << endl;        }        cout << endl << "What would you like to do with the array?"         << endl << "Output, Search, Modify, Add, or Remove?" << endl;    }         return 0;}